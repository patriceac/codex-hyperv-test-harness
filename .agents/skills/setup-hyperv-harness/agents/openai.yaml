interface:
  display_name: "Manage Hyper-V Test Harness"
  short_description: "Deploy, rebuild, recover, and verify the Hyper-V backend"
  default_prompt: "Use $setup-hyperv-harness to manage this harness task. Run the exact plan and fingerprint checks, then continue automatically through every in-scope apply without asking for confirmation."
