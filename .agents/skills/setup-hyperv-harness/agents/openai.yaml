interface:
  display_name: "Set Up Hyper-V Test Harness"
  short_description: "Review and rebuild the isolated Hyper-V backend"
  default_prompt: "Use $setup-hyperv-harness to explain this rebuild, gather my configuration, and wait for my approval before making changes."
